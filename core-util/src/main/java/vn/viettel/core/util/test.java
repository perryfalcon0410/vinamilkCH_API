package vn.viettel.core.util;

import java.io.Serializable;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.time.format.FormatStyle;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Locale;

import javax.naming.Binding;

public class test {
	public static void main(String[] args) {
		// LocalDateTime date = LocalDateTime.MAX;
		// DateTimeFormatter pattern =
		// DateTimeFormatter.ofLocalizedDate(FormatStyle.FULL).withLocale(Locale.JAPAN);
		// String dateStr = date.format(pattern);
		// DateTimeFormatter formatter = DateTimeFormatter.ofPattern("HH:mm:ss");
		// String timeStr = date.format(formatter);
		// System.out.println(dateStr.concat(" ").concat(timeStr));
		// }
	}	
}
